import React from 'react'
import Modal from 'components/Modal'

const AddIssue = ({ open, onClose }) => (
  <Modal open={open} onClose={onClose}>
    <div>das</div>
  </Modal>
)

export default AddIssue
